<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main1"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.invbikev1.ekranStarotwy.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/wyszukiwania"
        android:name="com.example.invbikev1.Itemy.Wyszukiwania"
        android:label="ekran_wyszukiwania"
        tools:layout="@layout/ekran_wyszukiwania" />
    <action
        android:id="@+id/onClick"
        app:destination="@id/wyszukiwania" />
</navigation>